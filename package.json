{
  "name": "dopbase",
  "private": true,
  "version": "0.1.7",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "bun ./scripts/dev.ts",
    "dev:ui": "vite",
    "dev:app": "bun run prepare:app && cargo run --manifest-path app/Cargo.toml -- server start",
    "dev:docs": "vitepress dev docs",
    "prepare:app": "test -f dist/index.html || bun run build:ui",
    "build": "bun run build:app && bun run build:docs",
    "build:ui": "bun run typecheck:ui && vite build",
    "build:app": "bun run build:ui && cargo build --manifest-path app/Cargo.toml --release",
    "build:docs": "rm -rf build && vitepress build docs && bun ./scripts/docs-postbuild.mjs",
    "test": "bun run test:ui && bun run test:app",
    "test:ui": "bunx vitest run --configLoader runner",
    "test:app": "bun run prepare:app && cargo test --manifest-path app/Cargo.toml --locked --all-targets && cargo test --manifest-path app/Cargo.toml --locked --all-targets --all-features",
    "test:docs": "bun scripts/check-mermaid-viewer.mjs",
    "test:github": "node --test scripts/github-templates.test.mjs",
    "test:installer": "sh scripts/install.test.sh",
    "clean": "cargo clean --manifest-path app/Cargo.toml",
    "lint": "bun run lint:ui && bun run lint:app",
    "lint:ui": "bunx eslint src",
    "lint:app": "bun run prepare:app && cargo clippy --manifest-path app/Cargo.toml --locked --all-targets --all-features -- -D warnings",
    "typecheck": "bun run typecheck:ui && bun run typecheck:app",
    "typecheck:ui": "bun run typecheck:ui:tsc && bun run typecheck:ui:vue",
    "typecheck:ui:tsc": "tsc -b --incremental false",
    "typecheck:ui:vue": "vue-tsc -b",
    "typecheck:app": "bun run prepare:app && cargo check --manifest-path app/Cargo.toml --locked --all-targets --all-features",
    "format:check": "bun run format:repo:check && bun run format:app:check",
    "format:repo:check": "bunx prettier --check .",
    "format:app:check": "cargo fmt --manifest-path app/Cargo.toml -- --check",
    "preview:ui": "vite preview",
    "preview:docs": "vitepress preview docs",
    "docker:test": "docker build -f app/Dockerfile.test -t dopbase-backend-test ."
  },
  "dependencies": {
    "@fontsource-variable/fira-code": "^5.3.0",
    "@tailwindcss/vite": "^4.3.3",
    "pinia": "^4.0.3",
    "tailwind-merge": "^3.6.0",
    "tailwindcss": "^4.3.3",
    "vue": "^3.5.41",
    "vue-i18n": "11",
    "vue-router": "^5.3.0"
  },
  "devDependencies": {
    "@typescript/native": "npm:typescript@~7.0.2",
    "@types/node": "^24.13.3",
    "@vitejs/plugin-vue": "^6.0.8",
    "@vitest/coverage-v8": "^4.1.11",
    "@vitest/ui": "^4.1.11",
    "@vue/tsconfig": "^0.9.1",
    "eslint": "^10.9.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-vue": "^10.10.0",
    "fastdom": "^1.0.12",
    "globals": "^17.11.0",
    "happy-dom": "^20.11.10",
    "mermaid": "^11",
    "prettier": "^3.9.6",
    "typescript": "npm:@typescript/typescript6@~6.0.2",
    "typescript-eslint": "^8.68.0",
    "vite": "^8.2.2",
    "vitepress": "^2.0.0-alpha.19",
    "vitepress-mermaid-viewer": "^0.4.0",
    "vitest": "^4.1.11",
    "vue-eslint-parser": "^10.4.1",
    "vue-tsc": "^3.3.11"
  }
}
